# smocket 데모 구현 계획 — `/demo`

> 작성: 2026.08.05 (수정 지시 반영)
> 짝 문서: `smocket_데모앱_기획_v2_2026-08-05.md` (사양의 출처. 충돌하면 기획서가 우선)
> 성격: 그 기획서를 어떤 순서로 무슨 코드로 짓는지의 계획. 사양을 새로 만들지 않는다.

---

## 0. 착수 전 확인한 것

기획서 §4-1·§5가 전제한 smocket API를 최신 `main`(`54e0c01`)에서 확인했다. npm 배포본(0.3.0)과 로컬 사본(0.2.1)에는 없고 `main`에만 있는 것들이라, **소비 경로가 `main` 빌드본이어야 성립한다.**

| 기획서 전제                                      | `main` 상태                                    |
| ------------------------------------------------ | ---------------------------------------------- |
| `SmocketAdapter.scheduleDelivery?(sid, deliver)` | 있음 (0018로 문서화)                           |
| `DelayingAdapter.setDelay(sid, ms)`              | 있음. `index.ts`에서 export                    |
| `socket.onAnyOutgoing` / `offAnyOutgoing`        | 있음. `Emitter`에 있으므로 서버·클라 양쪽 소켓 |

부수 확인:

- 빌드 통과 (tsup, ESM+CJS+dts).

> **정정 (8/5).** 여기 원래 *"`dist`에 node 빌트인 의존 0개 — 브라우저에서 그대로 돈다"*고 적었는데 **틀렸다.**
> `newId`가 `node:crypto`를 쓰고 있었고, 브라우저에서 `Buffer` 심이 `base64url`을 몰라서 던졌다.
> 소켓 id 생성이라 **클라이언트가 한 개도 연결되지 않았다.** npm 0.3.0에도 있던 문제다.
>
> 내 grep이 `from "node:`와 `require(`만 봐서 놓쳤다 — tsup이 `node:` 접두사를 떼고 `from 'crypto'`로 내보낸다.
> 앞으로 이런 감사는 **모듈 지정자 전체를 뽑아서** 본다:
>
> ```
> grep -ohE "from ['\"][^'\"]+['\"]|require\(['\"][^'\"]+['\"]\)" dist/index.js dist/index.cjs | sort -u
> ```
>
> smocket #139로 보고하고 #140에서 고쳤다 (Web Crypto + 손으로 짠 url-safe base64).
> 그 결과 **번들의 외부 모듈이 0개가 됐다.** 재감사 결과 다른 host 의존은 없다.
> 기획서 §8의 "웹소켓 연결 0개"는 브라우저에서 실측 확인했다 (외부 origin도 0개).
>
> 이게 §4-1·§11이 정한 "`src/` 수정이 필요하면 먼저 보고"의 첫 실사용이었고, 승인 후 진행했다.

### 0-1. 배달식 표기 통일 — 문구 교체 작업

`BroadcastOperator`의 메서드는 `to` / `timeout` / `emit`뿐이라 **`except()`가 없다.** 기획서 §4의 `io.to('room-1').except(sid_A)`는 지금 호출되지 않는다.

> **변동 예정.** `io.to().except()` 체이닝은 smocket 쪽 별도 작업으로 **병행 중**이라, 개발 기간 동안 이 절의 전제가 바뀔 수 있다. 착수 시점의 `main`을 다시 확인할 것. 체이닝이 들어오면 데모의 스트로크 호출을 `io.to('room-1').except(sid_A)`로 바꾸고 랜딩 카피를 원래 문구로 되돌리는 선택지가 생긴다 — 그때 정한다. 그 전까지 데모는 아래 형태를 쓴다.

**확정 표기.** 위는 실제 호출식, 아래는 그 호출의 도달 결과. `except`는 결과 표기 쪽에 남는다:

```
socket_A.to('room-1').emit('stroke', {…})
  → B, C  (except A)
```

이 형태로 **트레이스와 랜딩 카피를 통일한다.** 감수하고 넘어가는 게 아니라 고치는 작업이다:

- `content/landing.ts:39` — `trace.blocks[0].call`을 위 형태로 교체 (0단계에 포함)
- README에 같은 표현이 있는지 grep. 확인 결과 smocket README에는 `except(` 없음, 사이트 README에도 없음
- `app/components/DriftWords.tsx:15`의 `.except(sid)`는 호출식이 아니라 떠다니는 키워드 조각이고 `io.except(sid)`로 실재하므로 그대로 둔다

---

## 1. 트레이스 데이터 출처 — 확정

기획서 §4-1이 "adapter만으로는 이벤트명·payload가 안 보인다"고 남긴 미결을, `onAnyOutgoing`으로 닫는다.

`BroadcastOperator.emit`의 내부 순서를 소스에서 확인했고, **한 브로드캐스트가 전부 동기로** 이 순서다:

```
adapter.socketsIn(targetRooms)          ← 대상 집합
adapter.socketsIn(exceptRooms)          ← 제외 집합
각 수신자.emit() → emitOutgoing(event, args)   ← 이벤트명 · payload · 실제 도달 소켓
```

그래서 세 출처를 겹쳐 트레이스를 만든다:

| 줄 종류                             | 출처                                  |
| ----------------------------------- | ------------------------------------- |
| 대상 방 · 제외 집합 (라우팅 결정)   | `TraceAdapter.socketsIn` 오버라이드   |
| 이벤트명 · payload · 실제 도달 소켓 | 소켓별 `onAnyOutgoing`                |
| 방 입장 · 이탈                      | `TraceAdapter.add` / `del` 오버라이드 |
| 호출식 (`socket_A.to('room-1')`)    | **게임 코드가 넘긴다** — §1-1 규칙 4  |
| ack 응답                            | **게임 코드가 넘긴다** — §1-2         |

**라우팅 결정은 전부 smocket이 준다.** 데모가 도달·제외를 계산하는 자리는 한 군데도 없고, 게임 코드가 넘기는 건 자기가 뭘 불렀는지의 라벨뿐이다. 기획서 §4-1의 원칙을 지킨다. `src/` 수정이 필요 없으므로 8/17 프리즈에 보고할 것이 없다.

### 1-1. 조립 규칙 (구현 시 지킬 것)

1. `socketsIn` 첫 호출에서 "열린 배달"을 시작하고, 뒤따르는 `onAnyOutgoing` 호출들이 수신자를 append한다. 마이크로태스크에서 flush.
2. `onAnyOutgoing`은 **수신자마다 한 번씩** 불린다. 같은 `(event, args)` 묶음을 한 배달로 접어야 한다.
3. `targetRooms`가 비면(`io.emit` / `socket.broadcast`) `socketsIn`이 **1회만** 불린다. 이 데모는 항상 `io.to('room-1')` 계열이라 걸릴 일이 없지만 방어한다.
4. **호출식은 어댑터에서 복원할 수 없다.** 어댑터는 방 이름 집합만 받지 어떤 형태로 불렸는지는 모른다. 그러니 **게임 코드가 배달을 발생시킬 때 호출 형태를 스토어에 같이 넘긴다.** 게임 코드는 어차피 자기가 뭘 불렀는지 알고, ack 줄(§1-2)에서 이미 같은 방식을 쓴다.
   - 넘어오는 건 라벨이고, **도달·제외는 여전히 어댑터가 준다.** 둘이 섞이지 않는 한 §4-1 원칙은 유지된다.
   - 다만 §7의 "모든 배달식이 실제로 부른 식이다"는 이제 관측이 아니라 주장이 된다. 스토어에서 값싼 대조를 건다 — 게임 코드가 넘긴 호출식의 방 이름이 어댑터가 본 `targetRooms` / `exceptRooms`와 어긋나면 개발 중에 드러나게 한다.
5. **게임 코드는 targeted emit을 `socketA.emit()`이 아니라 `io.to(sid_A).emit()`으로 쏜다.** 소켓의 직접 `emit`은 어댑터를 타지 않아 라우팅 결정이 트레이스에 안 남는다.
6. 유저가 쏜 것도 찍혀야 하므로(기획서 §4) **클라이언트 소켓에도 `onAnyOutgoing`을 건다.** 서버로 들어가는 방향이 여기서 잡힌다.
7. **`add(sid, room)` / `del(sid, room)`도 오버라이드한다.** join은 emit이 아니라서 `socketsIn`에도 `onAnyOutgoing`에도 안 잡힌다. 두 훅이 방 입장·이탈 줄의 유일한 출처이고, 초기화 로그의 `A joined room-1`이 여기서 나온다.
   - 확인: `ServerSocket.join` → `nsp.adapter.add`, `leave` → `nsp.adapter.del`.
   - 덤: disconnect teardown도 방마다 `nsp.adapter.del`을 부르므로, **이탈 시나리오의 방 명단 정리도 `del` 훅이 대준다.**

### 1-2. ack 응답 — 게임 코드가 직접 기록, 시각적으로 구분

`emitOutgoing`은 args 끝의 콜백을 떼고, ack **응답** 자체는 어댑터도 훅도 타지 않는다. 그래서 게임 코드가 콜백 실행 시점에 `trace-store`에 직접 기록한다.

**단 관측된 배달과 시각적으로 구분한다:**

- 화살표 방향을 뒤집는다 — 도달은 `→ B, C`, ack는 `← ack`
- 색을 죽인다

섞이지만 않으면 §4-1 원칙(데모가 라우팅을 계산하지 않음)은 유지된다.

### 1-3. 예약 이벤트 — 별도 경로

`emitOutgoing`은 `RESERVED_EVENTS`(`connect` / `connect_error` / `disconnect` / `disconnecting`)면 즉시 리턴한다. **connect/disconnect 계열은 `onAnyOutgoing`에 안 잡힌다.**

따라서:

- 초기화 로그의 `client X connected` 줄 — 별도 경로 (연결 시점에 게임 코드가 기록)
- 이후 붙일 이탈 시나리오의 disconnect 이벤트 줄 — 별도 경로
- 단 두 경우 모두 **방 명단 변화는 `add`/`del` 훅이 대준다** (§1-1 규칙 7)

### 1-4. 리셋 경로 — 같은 url로 `new Server(url)`

시선 전환 = 라운드 재시작인데, smocket 사정이 이렇다:

- `io.close()` 미구현
- `resetRegistry()`는 존재하나 **test-only이고 패키지 index에서 의도적으로 미export** (독스트링에 명시) → 데모가 못 쓴다

**따라서 같은 url로 `new Server(url)`를 다시 만든다.** 생성자의 `servers.set(origin, this)`가 레지스트리를 덮어쓰므로 이후 `connect(url)`은 새 서버로 붙는다.

전환 시 직접 정리할 것:

- 이전 인스턴스와 소켓은 버린다
- **남은 타이머는 직접 끈다** — 재생 스케줄러, 프론트 라운드 타이머

---

## 2. 소비 경로 — tarball (임시)

정식 배포 전까지의 임시 경로.

```
smocket 레포에서   pnpm build && pnpm pack
                 → smocket-site/vendor/smocket-<ver>.tgz  (커밋)
smocket-site에서   pnpm add ./vendor/smocket-<ver>.tgz
```

- 심링크가 아니라 실제 설치라 기획서 §11의 "설치하면 진짜 돈다" 취지를 유지하고, Vercel에서 그대로 빌드된다.
- smocket이 나갈 때마다 재팩이 필요하므로 사이트에 동기화 스크립트를 둔다. 형제 경로 전제라 **임시임을 주석으로 명시**한다.

**마이그레이션은 선택이 아니다.** 정식 배포 후 `vendor/*.tgz`가 남으면 기획서 §11의 "설치하면 진짜 돈다"가 조용히 깨진다. §7 완료 기준의 마지막 줄(`package.json` 의존성이 `vendor/` 경로가 아닐 것)이 강제 장치다.

---

## 3. 파일 구조

```
app/demo/
  layout.tsx            다크 테마 CSS 임포트 + per-route viewport/metadata
  page.tsx              'use client' 진입점 (번들을 여기 가둔다, §8)
  demo.css              다크 토큰 — globals.css의 크림 토큰을 이 라우트에서만 덮음
  components/
    BootLog.tsx         로딩 = 초기화 로그 (스켈레톤 금지, §8)
    Countdown.tsx       3, 2, 1, 시작!!
    Stage.tsx           시선 전환 컨테이너 (§1-4의 리셋을 여기서 수행)
    Canvas.tsx          캔버스 — 그리기/재생 공용
    Desk.tsx            캐릭터 + 책상 + 말풍선
    ChatInput.tsx
    TracePanel.tsx      우상단
    ControlPanel.tsx    상황 패널
  lib/
    room.ts             게임 서버 로직 (Server 생성, join, word, stroke, chat, correct ack)
    trace-adapter.ts    TraceAdapter extends Adapter — socketsIn / add / del 오버라이드
    trace-store.ts      배달식 조립 + 폴딩(stroke ×47) + ack 줄 + 구독
    record.ts           녹화 포맷 + 기록기
    playback.ts         재생 스케줄러
    seed.json           임시 녹화 시드
vendor/
  smocket-<ver>.tgz
```

랜딩(`/`)은 서버 컴포넌트로 그대로 두고, 클라이언트 번들은 `/demo`에만 실린다.

---

## 4. 단계

8/10 최소선이 "녹화 한 세션이 smocket으로 끝까지 도는 상태"(기획서 §11)라, 녹화가 되려면 그리는 사람 시선이 먼저 있어야 한다. 기획서 서술 순서(관찰자 먼저)와 반대로 짓는다.

### 0. 기반

- vendor tarball + 설치 + 동기화 스크립트
- `/demo` 라우트 + 다크 테마 스코프 (랜딩 크림 유지, 번들 분리)
- **`content/landing.ts:39` 배달식 표기 교체** (§0-1)
- 랜딩 `Demo.tsx` 플레이스홀더를 `/demo` 입구로 교체 (기획서 §11). 스크린샷은 아직 없으므로 `TODO(hyun)`로 노출한다 (구조 지시서 §5-2)

### 1. 통신 코어 — 화면 없이 트레이스만 도는 상태

- `TraceAdapter` (`socketsIn` / `add` / `del`) + `trace-store` (§1-1 ~ §1-3 규칙대로)
- 게임 서버 로직: room-1 입장 / `word` targeted / `stroke` broadcast / `chat` room broadcast / 정답 ack
- 검증: 콘솔에 배달식이 도달·제외까지 맞게 찍히고, join 줄과 ack 줄이 각자 출처에서 나온다

### 2. 그리는 사람 시선

- 캔버스 입력 → stroke emit → 트레이스에 `→ B, C (except A)`
- B·C 화면은 보여주지 않는다 (기획서 §2-2). 도달 증명은 트레이스가 한다
- 봇 반응은 획 수·경과 시간 트리거의 오답 몇 번 → 정답

### 3. 녹화·재생 엔진 — **8/10 최소선**

- 이벤트 단위 녹화 포맷 + 기록/내보내기 (개발용, 못생겨도 됨)
- 재생 스케줄러. 재생도 진짜 smocket 배달 경로를 타므로 트레이스는 별도 처리 불필요
- 임시 시드 JSON으로 먼저 돌리고, 기현이 실제 한 판 그리면 교체

### 4. 관찰자 시선

- 중앙 상단 캔버스 + 좌하·우하 캐릭터/책상 + 머리 위 말풍선
- 봇 채팅: 시간 트리거로 스크립트 오답 문자열
- **유저 정답 vs 녹화본:** 유저가 정답 단어를 먼저 치면 정답 처리한다. **녹화본 재생은 중단하지 않고 끝까지 흐른다.** 봇이 뒤늦게 오답을 치는 그림이 되는데, 봇이라는 걸 드러내는 쪽이 낫고 종료 조건과 재생 스케줄러를 엮지 않아도 된다

### 5. 패널 · 연출

- 트레이스 패널 UI (우상단, JetBrains Mono, `stroke ×47` 접기, 게임 진행 이벤트는 항상 펼침, ack 줄은 `← ack`로 죽인 색)
- 상황 패널 — **확정 2개만**: 시선 전환 / 제시어 보기
- 카운트다운, 종료 조건 2종(정답 문자열 일치 / 프론트 타이머), 정답 순간 배달 3종이 트레이스에 세 줄로 구분되어 찍히기

### 6. 마감

- 반응형, 접근성(포커스 링·대비), 폰트

---

## 5. 별도 단계 — 추가 개발 (본 구현 범위 밖)

기획서 §5의 [제안] 항목들. **위 6단계에 포함되지 않는다.** 각각 독립이라 골라서 나중에 붙인다.

| 항목                     | 메모                                                                                                                                                                       |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B 수신 지연 슬라이더     | `DelayingAdapter.setDelay(sid, ms)`의 라이브 시연. `TraceAdapter`를 `extends DelayingAdapter`로 승격하면 트레이스와 지연을 한 어댑터가 겸한다                              |
| 그리는 사람 이탈 버튼    | A가 disconnect → 라운드 중단. 방 명단 정리는 `del` 훅이 대주고(§1-1 규칙 7), disconnect 이벤트 줄만 별도 경로(§1-3)                                                        |
| 재생 속도 ×0.5 / ×1 / ×2 | **×0.5가 핵심.** ×1에서는 스트로크가 쏟아지는 동안 트레이스 줄을 눈으로 못 따라간다. 지연 슬라이더와 같이 켜면 `setDelay`는 실시간 ms라 배속에 지연도 스케일할지 정해야 함 |
| 사운드 토글              | 기획서 §10. CC0/CC-BY 음원 확보가 선행                                                                                                                                     |

유실(확률적 드롭) 트리거는 넣지 않는다 — 기획서 §5의 판단 유지.

---

## 6. 시각 톤

기획서 §9는 [제안·미확정]이었고, 8/5 대화에서 확정됐다.

- 랜딩의 크림 라이트와 **반대이되 이어지는** 톤: 딥 스페이스 다크
- 액센트 오렌지 `#f4a259`는 랜딩과 공유한다 — 같은 세계라는 신호
- 앱 영역(캔버스·캐릭터·채팅)은 중성. 브랜드 색을 입히지 않는다
- A/B/C 소켓 3색을 정하고 **트레이스 라벨과 화면 요소(말풍선·커서)에 같은 색.** 오렌지는 피한다
- 랜딩 연출(별자리·s'more 드리프트·스크롤 효과)은 가져오지 않는다
- 코드 폰트: JetBrains Mono

---

## 7. 완료 기준

### 8/10 최소선

- [ ] 녹화 한 세션이 smocket 배달 경로를 타고 끝까지 돈다
- [ ] 트레이스에 배달식이 도달·제외까지 맞게 찍힌다
- [ ] smocket `src/`를 수정하지 않았다

### 그 이후

- [ ] `/demo` 진입 → 초기화 로그 → 3,2,1 → 라운드
- [ ] 네트워크 탭 웹소켓 0개, 외부 요청 0개
- [ ] 트레이스의 모든 배달식이 실제로 부른 식이다
      — 호출식은 게임 코드가 넘긴 라벨이므로(§1-1 규칙 4) 스토어의 대조가 통과하는지로 본다.
      **`except` 체이닝 작업이 병행 중이라 "실제로 부른 식"이 무엇인지가 개발 중에 바뀔 수 있다** —
      확인은 그 시점의 smocket `main`을 기준으로 한다 (§0-1 변동 예정)
- [ ] 두 시선 전환, 발신자 변경이 제외 표기에 나타난다
- [ ] 랜딩 크림 톤·번들이 데모에 오염되지 않는다
- [ ] `package.json` 의존성이 `vendor/` 경로가 아니다
