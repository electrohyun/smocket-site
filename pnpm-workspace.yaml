# 이 랜딩 페이지는 이미지 최적화(sharp)를 쓰지 않고, 네이티브 리졸버 빌드도 필요 없다.
# pnpm 11의 빌드 스크립트 승인: tsx의 esbuild만 실행하고 이미지/리졸버 빌드는 실행하지 않는다.
allowBuilds:
  esbuild: true
  sharp: false
  unrs-resolver: false
minimumReleaseAgeExclude:
  - smocket-client@1.0.0
  - smocket@1.0.0
