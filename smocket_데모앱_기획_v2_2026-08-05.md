# smocket 데모 앱 기획 v2 — 그림 대결

> 작성: 2026.08.05
> **대체 대상: 2026-08-04 기획서.** 충돌하는 항목은 이 문서가 우선.
> 위치: smocket-site 레포, **`/demo` 별도 라우트** (랜딩 섹션 아님)
> 성격: Claude Code 위임용. §"미결·재량" 외에는 확정 사양.
> 표기 규칙: **[재량]** = Claude Code가 알아서 정함 / **[제안]** = Claude가 낸 안, 기현 확정 아님

---

## 0. 원칙 (유지)

**주인공은 앱이 아니라 배달 내역이다.** 앱은 못생겨도 되고, 배달이 선명해야 한다.

이건 본격적인 게임이 아니라 **데모다.** 게임 로직을 진지하게 만들지 않는다. 정답 판정도, 점수도, 서버 권위도 없다.

**데모는 바로 보여야 한다.** 실시간 서비스가 즉시 뜨는 것 자체가 socket.io 모킹 도구의 증명.

---

## 1. 형식

그림 대결(그리기+맞히기 파티게임 구조). "캐치마인드"/"Pictionary"는 상표라 이름으로 쓰지 않는다.

**한 라운드로 끝.** 라운드 로테이션·재대결 없음. 정답이 나오거나 타이머가 끝나면 상황 종료, 그 뒤 아무것도 없음.

등장 소켓 3개: 그리는 사람 A + 맞히는 사람 B, C.

---

## 2. 두 가지 시선

데모 진입 시 두 시선 중 하나로 본다. (선택 UI는 §5 상황 패널)

### 2-1. 관찰자 시선 (기본)

- 맞히는 사람의 화면. 재생 전용.
- **그림은 AI(재생 엔진)가 그린다** — 기현이 미리 그린 원본을 소켓 이벤트 단위로 녹화해 두고, 그 녹화본을 스케줄대로 emit해서 재생. (녹화 포맷은 §6)
- 화면 구성: 중앙 상단 크게 캔버스(A의 그림이 도착해서 그려짐), 왼쪽 아래·오른쪽 아래에 **캐릭터 + 책상**. 채팅을 치면 말풍선이 캐릭터 머리 위에 뜬다.
- 두 자리 중 하나는 다른 관찰자(봇). 봇은 **진짜 사람 같지만 랜덤한 채팅을 내뱉는다.** 그림을 이해하는 척할 필요 없음 — 스크립트에 박힌 오답 문자열을 시간 트리거로 내보내면 됨.
- 나머지 한 자리가 유저 본인. 채팅 입력 가능.

### 2-2. 그리는 사람 시선

관찰자 시선의 반대. 유저가 직접 캔버스에 그린다.

- 제시어가 유저에게 보인다 (targeted emit으로 도착).
- 유저가 그린 스트로크가 소켓 이벤트로 나가고, 트레이스에 `→ B, C (except A)`가 찍힌다.
- **B·C의 화면을 동시에 보여주지 않는다.** 도착 증명은 트레이스 패널이 한다. (도착하는 쪽 화면을 실제로 보여주는 건 관찰자 시선의 역할 — 두 시선이 송신/수신을 나눠 맡는 구조)
- B·C의 채팅 반응: **[재량]** — 봇 스크립트. 제시어는 시스템이 아니까 유저가 뭘 그리든 성립함. 획 수·경과 시간 트리거로 오답 몇 번 → 정답, 정도면 충분.

---

## 3. 게임 흐름

1. `/demo` 진입 → **로딩 잠깐 → 바로 시작.** 입구 화면·재생 버튼·정지 스냅샷 없음.
   - 로딩 동안 트레이스 패널에 초기화 로그가 흐른다 (8/4 기획 §5-3 유지):
     ```
     new Server()
     client A connected   sid_a3f1
     client B connected   sid_b7c2
     client C connected   sid_c9e4
     A joined room-1
     ...
     ```
2. **3, 2, 1, 시작!!** 카운트다운.
3. 라운드 진행. 그리기 + 채팅.
4. 종료 조건 둘 중 하나:
   - **정답:** 정해진 단어를 채팅에 입력하면 정답. **정답 판정 로직 없음** — 문자열 일치면 끝. (데모니까)
   - **타임아웃:** 타이머 만료. **프론트 시간으로만 잰다.** 서버 계산·UTC 동기화 같은 것 없음. 진지하게 만들지 말 것.
5. 끝. 뭐 없음.

정답 순간의 연출과 배달 3종(ack 콜백 / 정답자 targeted emit / 방 전체 공지 broadcast)의 화면 표현: **[재량].** 단, 트레이스에는 세 줄이 구분되어 찍혀야 함.

---

## 4. 트레이스 패널 (8/4 기획 유지 + 위치 확정)

**오른쪽 상단.** 이 데모의 모든 소켓 통신이 — 유저가 쏜 것 포함 — 계속 찍힌다.

배달식 통째로:

```
io.to('room-1').except(sid_A).emit('stroke', {…})
  → B, C  (except A)

io.to(sid_A).emit('word', '기린')
  → A

io.to('room-1').emit('chat', {…})
  → A, B, C
```

세부 규칙 (8/4 기획 §3-1 그대로):

| 항목             | 규칙                                        |
| ---------------- | ------------------------------------------- |
| 소켓 식별자      | 라벨(A/B/C) + 실제 sid 앞 4자리 흐리게 병기 |
| 제외 대상        | `→ B, C (except A)` — 제외된 쪽도 표기      |
| 스트로크 폭주    | 연속 동일 이벤트는 `stroke ×47`로 접기      |
| 게임 진행 이벤트 | join/word/chat/correct는 항상 펼침          |
| 폰트             | JetBrains Mono                              |

**제시어 payload:** 관찰자 시선에서 `word` 이벤트의 payload는 기본적으로 가려짐(`'****'` 등). **상황 패널에서 유저가 "제시어 보기"를 켤 수 있다** — 그러면 그림 그려지는 걸 알고 보는 재미가 생김.

### 4-1. 트레이스 데이터 출처 — 구현 방식

smocket의 어댑터 등록 API 위에 트레이스 adapter를 얹는다. 데모용 UI 코드로 라우팅을 따로 짜지 않는다 (라이브러리와 데모가 분리되면 확장점 실증이 사라짐).

확인된 실제 API (smocket v0.3.0+, `src/contract.ts`):

```ts
interface SmocketAdapter {
  rooms: Map<string, Set<string>>;
  sids: Map<string, Set<string>>;
  add(sid: string, room: string): void;
  del(sid: string, room: string): void;
  socketsIn(rooms: Iterable<string>): Set<string>; // 라우팅 결정
  scheduleDelivery?(sid: string, deliver: () => void): void; // 선택 훅
}
// 등록: io.adapter((nsp) => new TraceAdapter(nsp))
// 기본 구현 상속 가능: class TraceAdapter extends Adapter { ... }
```

**알려진 제약:** 코어가 브로드캐스트당 `socketsIn`을 두 번 호출한다 (target rooms → except rooms, 빼기는 코어에서). 따라서 adapter만으로는 방·sid 집합은 보이지만 **이벤트명·payload가 안 보인다.** 배달식 한 줄을 완성하려면 이벤트명 출처를 합쳐야 함 — 후보는 `socket.onAnyOutgoing` / `onAny` (v0.4.0). **[재량]** — 조합 방식은 구현하면서 정하되, smocket `src/` 수정이 필요해지면 **구현하지 말고 먼저 보고할 것** (8/17 프리즈 관련 결정 필요).

---

## 5. 상황 패널

시선·상황을 유저가 고르는 컨트롤 패널. 위치·형태 **[재량]** (트레이스 패널과 겹치지 않게).

확정 항목:

| 항목        | 동작                                     |
| ----------- | ---------------------------------------- |
| 시선 전환   | 관찰자 ↔ 그리는 사람                     |
| 제시어 보기 | 관찰자 시선에서 `word` payload 가림 해제 |

**[제안]** 추가 후보 — 상황 패널을 "배달 의미론 트리거 모음"으로 키우는 방향. 각각 독립이라 골라 넣으면 됨:

| 후보                 | 내용                                                                     | 근거                                                                                                                                               |
| -------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| **그리는 사람 이탈** | 버튼 누르면 A가 disconnect → 라운드 중단, 방 명단 정리가 트레이스에 찍힘 | 8/4 기획 커버리지 표에 있던 disconnect 시나리오의 부활. 파급이 커서 테스트 케이스로 강했던 항목                                                    |
| **B 수신 지연**      | 슬라이더로 B의 수신만 N ms 지연 → 관찰자 화면에서 B 캐릭터만 늦게 반응   | **smocket에 실제로 있는 `DelayingAdapter.setDelay(sid, ms)`를 그대로 씀.** 데모 연출이 아니라 shipped 기능의 라이브 시연이 됨. 후보 중 값어치 최상 |
| 재생 속도            | 관찰자 시선 녹화 재생 ×1 / ×2                                            | 시연 영상 촬영 때도 유용                                                                                                                           |
| 사운드 토글          | 기본 음소거 해제                                                         | 8/4 기획 §7 유지                                                                                                                                   |

**[제안]에 대한 판단 하나:** 유실(확률적 드롭) 트리거는 넣지 말 것 — 유실 adapter가 smocket에 미구현이라, 넣으면 데모 UI 코드로 배달을 조작하는 게 되어 §4-1 원칙에 어긋남.

---

## 6. 녹화·재생 (관찰자 시선의 엔진)

- **녹화:** 기현이 그리는 사람 시선(또는 별도 녹화 모드)에서 실제로 한 판 플레이 → 나가는 소켓 이벤트를 타임스탬프와 함께 기록 → JSON 등으로 저장. 포맷 **[재량]** — 단, 이벤트 단위여야 함 (프레임·픽셀 녹화 아님).
- **재생:** 녹화본의 스케줄대로 같은 이벤트를 emit. 재생도 진짜 smocket 배달 경로를 타므로 트레이스에 전부 찍힌다 — 별도 처리 불필요.
- 녹화 도구(기록 버튼·내보내기)는 기현만 쓰는 개발용이라 못생겨도 됨. 프로덕션 노출 여부 **[재량]**.

---

## 7. 배달 의미론 커버리지 (v2 기준 갱신)

| 의미론             | 자리                        | 상태                                                                                                      |
| ------------------ | --------------------------- | --------------------------------------------------------------------------------------------------------- |
| rooms              | room-1 입장 (초기화 로그)   | 확정                                                                                                      |
| broadcast + except | 스트로크를 발신자 빼고 전달 | 확정 — 게임 성립 조건 그 자체                                                                             |
| targeted emit      | 제시어를 그리는 사람에게만  | 확정                                                                                                      |
| room broadcast     | 채팅, 정답 공지             | 확정                                                                                                      |
| ack                | 정답 입력 → 콜백            | 확정 (표현은 [재량])                                                                                      |
| disconnect 정리    | 상황 패널 "이탈" 버튼       | **[제안]** — 채택 시 부활                                                                                 |
| 역할 비대칭        | 두 시선 그 자체             | 확정                                                                                                      |
| namespace 격리     | —                           | **제외.** 8/4 기획 §9-2 미결이었으나 설계적 필연성이 약했고, v2 구조(한 라운드·시선 2개)에 낄 자리가 없음 |

**발신자별 except 계산의 증명은 시선 전환이 담당한다** — 관찰자 시선에선 재생 엔진(A)이 발신자, 그리는 사람 시선에선 유저가 발신자. 라운드 로테이션은 이걸로 대체되어 폐기.

---

## 8. 로딩·성능

- `/demo` 라우트에 클라이언트 컴포넌트를 가둔다. 랜딩(`/`)은 서버 컴포넌트 유지, 번들 분리.
- 로딩 화면 = 초기화 로그 (§3-1). 스켈레톤 쓰지 말 것 — 로딩 자체가 smocket이 일하는 장면.
- **서버 없음 증거:** 네트워크 탭에 웹소켓 연결 0개. 시연 때 직접 열어 보이는 용도이므로, 데모가 외부 요청을 만들지 않도록 할 것 (에셋은 로컬).

---

## 9. 시각 톤

**[제안 — 미확정. 착수 전 기현 확인 필요]**

`/demo`가 별도 라우트가 되면서 8/4 기획 §6(차콜 다크)도, 랜딩의 크림 라이트도 자동 적용되지 않는다. 8/5 대화에서 나온 제안:

- 페이지 배경: **다크.** 흰 앱 카드·캔버스 스트로크의 대비, 트레이스 패널의 데브툴 톤, 영상 압축에서 크림이 뭉개지는 문제까지 해결
- 앱 영역(캔버스·캐릭터·채팅): 중성. 브랜드 색 입히지 말 것 — "너희 앱은 그대로 두고 smocket만 끼운다"는 주장과 일치해야 함
- 브랜드는 액센트만: 토스트 오렌지 `#f4a259`를 활성 상태·정답 순간 정도에, 로고 고양이는 좌상단 작게
- A/B/C 소켓 색 3개를 정하고 **트레이스 라벨과 화면 요소(말풍선·커서 등)에 같은 색** — 화면의 행위와 트레이스의 기록이 색으로 이어지는 게 핵심 연결고리
- 소켓 색에서 토스트 오렌지는 피할 것 (페이지 액센트와 역할 충돌)
- 랜딩의 연출(별자리·s'more 드리프트·스크롤 효과)은 가져오지 않는다. 캔버스와 시각적으로 싸움
- 코드 폰트: JetBrains Mono (트레이스 패널)

## 10. 사운드 (8/4 기획 §7 유지)

- CC0 / CC-BY로 먼저 좁히고 그 안에서 고름. "무료 브금" 유튜브 배포본 금지 (콘텐츠 ID → 시연 영상 차단 리스크)
- 기본 음소거, 토글 눈에 띄게
- 효과음 > BGM. 정답 순간의 짧은 소리 = targeted emit 도착의 청각 신호
- CC-BY 사용 시 크레딧: 푸터 + `THIRD_PARTY_NOTICES`

---

## 11. 위치·프리즈 (8/4 기획 §8 유지)

- smocket-site 레포. **smocket은 npm에서 실제 설치** (workspace 링크 금지 — "설치하면 진짜 돈다"의 증명)
- 데모는 smocket `src/`가 아니므로 8/17 기능 프리즈 대상 아님. 단 §4-1의 트레이스 구현이 `src/` 수정을 요구하면 프리즈 전 결정 필요 → 보고
- 랜딩의 기존 `Demo.tsx` placeholder 자리는 `/demo` 입구(스크린샷 + 링크)로 교체
- 8/10 = 최소선: 녹화 한 세션이 smocket으로 끝까지 돌아가는 상태. UI 다듬기는 이후 계속 가능

---

## 12. 한 장 요약

| 항목          | 결정                                                                      |
| ------------- | ------------------------------------------------------------------------- |
| 형식          | 그림 대결, **한 라운드**, 정답 or 타임아웃이면 끝                         |
| 시선          | 관찰자(재생, 기본) / 그리는 사람(실시간) — 상황 패널에서 전환             |
| 관찰자 화면   | 중앙 캔버스 + 좌하·우하 캐릭터+책상, 말풍선 채팅                          |
| 봇            | 랜덤 채팅. 정답 판정 로직 없음 — 정해진 단어 입력이 곧 정답               |
| 타이머        | 프론트 시간. 서버 계산 없음                                               |
| 트레이스      | 우상단. 모든 소켓 통신. 배달식+도달+제외. 제시어 payload는 상황 패널 토글 |
| 트레이스 출처 | smocket adapter API (`io.adapter`) + onAny 계열. `src/` 수정 필요 시 보고 |
| 상황 패널     | 시선 전환 + 제시어 보기 확정. 이탈 버튼·지연 슬라이더는 [제안]            |
| 시작          | 입구 없음. 진입 → 초기화 로그 로딩 → 3,2,1 시작                           |
| 톤            | [제안] 다크 배경 + 중성 앱 + 오렌지 액센트 — 착수 전 확인                 |
| 위치          | smocket-site `/demo`. npm 실설치. 8/10 최소선 = 녹화 세션 완주            |

**한 문장: 앱은 못생겨도 되고, 배달이 선명해야 한다. 그리고 이건 게임이 아니라 데모다.**
