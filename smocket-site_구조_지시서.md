# smocket-site — 랜딩 페이지 구조 구현 지시서

> 작성: 2026.08.04
> 대상: Claude Code
> 성격: 구현 지시. **섹션 순서·구성·구현 수준 판단은 Claude가 정리한 것이며 기현이 확정한 기준이 아님.** 현실과 어긋나면 버리면 됨.
> 짝 문서: `smocket-site_카피_원본.md` (모든 문구의 출처. 이 문서에 문구를 새로 쓰지 말 것)

---

## 0. 이번 라운드의 목적

**구조를 눈으로 확인하는 것.** 시각 완성도, 애니메이션, 연출은 이번 범위가 아니다.

산출물은 **단일 페이지 1개**이고, 섹션이 명세된 순서대로 쌓여 있으며, 각 섹션에 실제 카피가 들어가 있고, 아직 없는 재료는 화면에 `TODO(hyun)`로 노출된 상태.

**보고 판단할 것:** 섹션 순서가 맞는지, 각 섹션이 자기 자리에서 할 일을 하는지, 빠지거나 남는 섹션이 있는지.

---

## 1. 전제

| 항목 | 값 |
|---|---|
| 레포 | `smocket-site` (신규) |
| 스택 | Next.js (App Router) + TypeScript |
| 배포 | Vercel 전제 (이번 라운드에서 배포 설정은 하지 않음) |
| 모드 | **다크 전용.** 라이트 모드 토글을 만들지 말 것 |
| 배경 | `#0f1115` |
| 강조 | `#f4a259` |
| 보조 | `#7bb0ff` |
| 코드/데이터 폰트 | JetBrains Mono |
| 본문 폰트 | **이번 라운드에서 확정하지 않음.** 시스템 산세리프로 두고 진행 |

추가 색은 위 3색과 회색 3~4단까지만. 그라디언트, 글로우, 네온 사용 금지.

---

## 2. 섹션 순서 (확정)

| # | id | 역할 | 이번 라운드 구현 수준 |
|---|---|---|---|
| 1 | `hero` | 한 줄 정의와 진입 경로 | 실물 |
| 2 | `trace` | **첫 증거.** 배달 내역을 보여줌 | 실물 (정지, 하드코딩) |
| 3 | `pain` | before / after 코드 병치 | 실물 (before 코드는 TODO) |
| 4 | `features` | 기능 카드 4개 | 실물 |
| 5 | `demo` | 그림 대결 데모가 들어갈 자리 | **플레이스홀더 박스** |
| 6 | `quickstart` | 설치 → 교체 → 테스트 실행 | 실물 |
| 7 | `scope` | 하는 것 / 못 하는 것 | 실물 |
| 8 | `footer` | 링크 | 실물 |

### 만들지 말 것 (의도적 제외)

스폰서, 기여자 아바타 그리드, 후기·인용, 사용처 로고 월, 비교표, 뉴스레터 구독, 다운로드 수·스타 수 배지.

**이유: 지금 채울 실물이 없다.** 빈 섹션이 보이는 것이 섹션이 없는 것보다 나쁘다. 나중에 재료가 생기면 그때 추가한다.

---

## 3. 섹션별 명세

### 3-1. `hero`

**포함**
- 워드마크: 텍스트 `smocket` (마스코트 이미지 자리는 점선 박스 + `TODO(hyun): mascot`)
- H1 (카피 문서 §1)
- 서브 1문장 (카피 문서 §1)
- CTA 2개: `Docs` → GitHub README 링크 / `GitHub` → 레포 링크
- 칩 한 줄: 버전 · 라이선스 · dual-run CI 상태

**금지**
- **install 명령어를 히어로에 넣지 말 것.** (MSW / Motion / Socket.IO / TanStack Query / Biome 5곳 모두 히어로에 없음)
- 큰 숫자 카운터, 스타 수, 다운로드 수

---

### 3-2. `trace` — 이 사이트에서 가장 중요한 섹션

**목적:** smocket이 파는 것은 "누가 받았는가"이므로, 첫 증거도 배달 내역이어야 한다.

**포함**
- 배달식 3블록. 각 블록은 `호출식` 다음 줄에 `→ 도달 목록 (제외 표기)`
- JetBrains Mono, 좌측 정렬, 등폭 정렬 유지
- 소켓 표기: 라벨 `A` `B` `C` + sid 앞 4자리를 흐린 회색으로 병기 (예: `A a3f1`)
- 제외된 소켓은 도달 목록 뒤 괄호로 명시

**내용 (하드코딩, 그대로 사용)**

```
io.to('room-1').except(sid_A).emit('stroke', { … })
  → B, C   (except A)

io.to(sid_A).emit('word', 'giraffe')
  → A

io.to('room-1').emit('chat', { … })
  → A, B, C
```

**금지**
- 재생 버튼, 타이핑 애니메이션, 자동 스크롤. **완전한 정지 상태.**
- 배달식을 지어내지 말 것. 위 3개만 사용.

---

### 3-3. `pain`

**포함**
- 좌우 2열. 좌 = `Before`, 우 = `After`
- 좌: 손으로 짠 MockSocket 발췌
  → **코드를 지어내지 말 것.** `TODO(hyun): 폴리카소 MockSocket 발췌 붙일 것` 주석과 함께 빈 코드 블록으로 둔다
- 우: smocket 사용 코드 (카피 문서 §3)
- 하단 캡션 1줄 (카피 문서 §3)

**금지**
- "N배 빠름", "N시간 절약" 류 문구. 측정한 적 없다.

---

### 3-4. `features`

카드 4개, 2×2. 각 카드 = 제목 1줄 + 설명 2줄 이내. 아이콘 없음.

내용은 카피 문서 §4.

---

### 3-5. `demo`

**이번 라운드에서는 만들지 않는다.**

- 16:9 비율의 점선 박스
- 안에 3분할 레이아웃 비율만 표시 (좌 대형 1 / 우 소형 2 + 트레이스 패널)
- 라벨: `TODO: drawing demo`
- 박스 위에 섹션 제목과 설명 1줄은 실물로 넣는다 (카피 문서 §5)

**목적:** 데모가 들어갈 자리의 크기와 위치를 페이지 안에서 확인하는 것.

---

### 3-6. `quickstart`

3스텝. 각 스텝 = 번호 + 제목 + 코드 블록.

1. 설치 — npm 실배포 전이므로 명령줄은 `TODO(hyun): install command`
2. 교체 — import 한 줄 바꾸기
3. 실행 — **실제로 통과하는 테스트 파일 전문**을 넣는다 (카피 문서 §6)

코드 블록에 복사 버튼을 붙인다. 여기가 이 페이지에서 복사 버튼이 있는 유일한 자리.

---

### 3-7. `scope`

2열 표. 좌 = 하는 것, 우 = 못 하는 것.

못 하는 것 5종의 서술 원칙: **"안 만든 것"이 아니라 "mock 환경에 존재할 수 없는 것"으로 쓴다.** 문구는 카피 문서 §7을 그대로 사용하고 바꾸지 말 것.

---

### 3-8. `footer`

- 링크: Docs / GitHub / npm / License
- 저작 표기 1줄
- npm 링크는 실배포 전이므로 `TODO(hyun): npm link`

---

## 4. 구현 규칙

1. **모든 문구를 `content/landing.ts` 한 파일에 상수로 모은다.** JSX에 문자열을 직접 쓰지 말 것. 순서와 문구를 바꿔가며 볼 것이므로 교체가 쉬워야 한다.
2. 각 `<section>`에 `id`와 `data-section` 속성을 붙인다.
3. **애니메이션 금지.** 허용 범위는 링크·버튼의 hover 색 변화까지.
4. 이미지 에셋 없음. 필요한 자리는 점선 박스 + 라벨.
5. 반응형: 1280 기준으로 작성, 768 이하에서 전부 1열. 그 사이 구간 최적화는 하지 않는다.
6. 접근성: 키보드 포커스 링을 지우지 말 것. 본문 대비 4.5:1 이상.
7. 외부 API 호출, localStorage, 분석 스크립트 없음.
8. 컴포넌트는 섹션당 파일 1개. 공용 UI 추상화를 미리 만들지 말 것.

---

## 5. 사실 규칙

1. **숫자, 인용, 회사 로고, 사용처를 지어내지 말 것.**
2. 모르는 값은 `TODO(hyun): 무엇이 필요한지` 문자열로 남기고 **화면에 그대로 노출**한다. 그럴듯한 임시값으로 채우지 말 것.
3. 성능·속도 비교 주장 금지. 벤치마크를 한 적이 없다.
4. 대회, 평가, 심사, 점수와 관련된 표현을 어떤 파일에도 쓰지 말 것 (코드 주석·커밋 메시지 포함).
5. 카피 문서에 없는 문구가 필요해지면, 지어내지 말고 `TODO(hyun): copy needed — <자리 이름>`으로 남긴다.

---

## 6. 완료 기준

- [ ] `pnpm dev`로 페이지 1개가 렌더된다
- [ ] 섹션 8개가 §2의 순서대로 존재한다
- [ ] 모든 문구가 `content/landing.ts`에서 온다
- [ ] 라이트 모드 토글, 스폰서, 후기, 비교표가 **없다**
- [ ] 애니메이션이 없다
- [ ] 레포 루트에 `TODO.md`를 생성하고, 코드 전체의 `TODO(hyun)` 항목을 파일·위치와 함께 목록으로 정리한다

---

## 7. 하지 말 것 (요약)

- 히어로에 install 명령어
- 지어낸 숫자·후기·로고
- 애니메이션, 스크롤 연출, 그라디언트
- 라이트 모드
- 공용 컴포넌트 추상화 선행
- 데모 실제 구현 (별도 사슬 작업)
- 카피 문서에 없는 문구 창작
